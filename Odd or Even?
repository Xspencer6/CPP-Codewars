#include <string>
#include <vector>

std::string odd_or_even(const std::vector<int> &arr)
{
  if(arr.empty()) return "even";
  
  int total = 0;
    // adds element first
  for(auto val : arr){
    total += val;
  }
  if(total % 2 == 0) return "even";
  else return "odd";
}

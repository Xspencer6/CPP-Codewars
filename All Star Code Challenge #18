#include <string>

unsigned int strCount(std::string word, char letter){
  unsigned int occurrences = 0;
  for(int i = 0; i < word.length(); i++){
    if (word[i] == letter) occurrences++;
  }
  return occurrences;
}

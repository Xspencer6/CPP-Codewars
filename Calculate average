#include <vector>

double calcAverage(const std::vector<int>& values){
  if (sizeof(values) < 0) return 0;
  
  float total = 0.0;
  for(int i = 0; i < values.size(); i++){
      total += values[i];    
  }
  return total/values.size();
}

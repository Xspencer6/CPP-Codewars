int points(const std::array<std::string, 10>& games) {
  int total = 0;
    for(auto elem : games){
      if (elem[0] > elem[2]) total += 3;
      if (elem[0] == elem[2]) total += 1;
    }
  return total;
}
